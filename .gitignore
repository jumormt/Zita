# Ignore all macOS .DS_Store files in any folder
**/.DS_Store
.DS_Store
.idea
target/
*.iml
.vscode/
*.log
*.class
*.jar
*.swp
*.bak

# Batch-pipeline output and ad-hoc submission corpora left behind by docker
# runs of scripts/batch_process_zita.py. Both are produced at runtime and
# never belong in version control.
out/
submissions/

# Project-local scratch directory (per CLAUDE.md / rules: workspace
# convention is `/temp` for ad-hoc fixtures, regression sweeps, and
# any other disposable intermediates).
temp/